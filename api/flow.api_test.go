package api

import (
	"testing"
)

func TestFlowAPI(t *testing.T) {


	// Connect Node Server
	/*t.Run("NodeServerが存在する時、接続に成功する", func (t *testing.T) {
		nodeAddr := "127.0.0.1:9990"
		flowAPI := NewFlowAPI()
		synerexServer, err := flowAPI.connectNodeServer(nodeAddr)
		assert.Equal(t, err, nil)
		assert.NotEqual(t, synerexServer, nil)
	})

	t.Run("NodeServerが存在しない時、エラーを発する", func (t *testing.T) {
		nodeAddr := "127.0.0.1:9990"
		flowAPI := NewFlowAPI()
		synerexServer, err := flowAPI.connectNodeServer(nodeAddr)
		assert.NotEqual(t, err, nil)
		assert.Equal(t, synerexServer, nil)
	})*/
}
